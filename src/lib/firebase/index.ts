// Export Firebase instances
export { app, db, rtdb } from './config';

// Export test utilities
export { testFirebaseConnection } from './test';

// Export Firebase types (if needed)
export type { 
  // Add any Firebase-related types here
}; 